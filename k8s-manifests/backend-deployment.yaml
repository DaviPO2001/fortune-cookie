apiVersion: apps/v1

kind: Deployment

metadata:
  name: backend
  namespace: default

spec:
  replicas: 1

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
        - name: backend

          image: 009160038179.dkr.ecr.us-east-1.amazonaws.com/registry:${IMAGE_TAG}

          ports:
            - containerPort: 3000